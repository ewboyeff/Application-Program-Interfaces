import { defineConfig } from "@lovable.dev/vite-tanstack-config";

// cloudflare: false — Vercel deployment uchun Cloudflare Workers plugini o'chirilgan
export default defineConfig({ cloudflare: false });
